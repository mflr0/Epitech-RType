#include "client/components/rotation_component.h"

RotationComponent::RotationComponent(const float &rota) {
    _rotation = rota;
}

void RotationComponent::setRotation(const float &rota) {
    _rotation = rota;
}
